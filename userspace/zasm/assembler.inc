;; Zicronix Assembler - A simple assembler for 80386.
;; The main assember file.
;; This does basically all the things, parses puke and then calls the appropriate instruction crap :D
;; Assembler init - Intialization of the assember called by Main()
assembler_init:
		;; Intialization of the assembler.
		
